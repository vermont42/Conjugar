# Conjugar Code Review — Recommendations (Round 2)

**Date:** July 15, 2026
**Scope:** The 96 commits since `f8d5714` (the round-1 implementation wrap-up) — the game **La Subida** and its **La Llamada** boss fight, the **onboarding** flow, the **audio/haptics stack**, **alternate app icons**, the **etymology + example-uses** content subsystems (≈4 MB of new bundled data), the **large-widget** rework, and the settings/router wiring that ties them together. The conjugation engine itself was diffed file-by-file: every engine change since round 1 is **comment-only** (the provenance-citation sweep), and `verbModelMap.xml` changed by one display gloss (`desvelar`) — so this round's conjugation-correctness risk lives in the new *content*, not the engine, and the content checks below reflect that.
**Baseline:** Build green. Full suite: **508 tests, 26 suites, 0 failures** (up from 403/17 at round 1 — the game alone added ~100). SwiftLint: **0 violations in 181 files**. Round-1 items were spot-checked as implemented; none are re-reported.
**Carve-outs honored:** the `ModelBrowseView`/`VerbBrowseView` mirroring, the planned-TelemetryDeck-only analytics, the legacy `flamencoLoop.mp3` / `CONJUGAR_GAME_DISABLE_FLAGS` names, `TutorTestView` shipping without `#if DEBUG`, Spanish-in-both-localizations jaleos/title cards, and the `nonisolated`-by-design engine are all left alone.

> **On line references:** `file:line` citations are accurate as of this writing and will drift as items are implemented. Treat each as a starting pointer, not a durable address — confirm by symbol and surrounding context before editing.

**Content-integrity checks run for this review (all clean):** every `~bold~` marker in all 2,690 etymology entries is balanced; the `en`/`es` tables have identical key sets (1,345 each); every etymology/example/medieval key resolves in `verbModelMap.xml`; all **988 frequency-ranked verbs have both an example and an etymology** (the widget's two new blocks can never be empty for a real snapshot); every example's `token` appears as a whole word in its Spanish sentence; every `source` maps to a known attribution (no `.other` fallback ships); all 2,139 medieval attestations carry a valid work code. Sampled entries (ser, ir, haber, desvelar, morder, oler + literary examples) read as fluent, accurate Spanish with faithful translations. The licensing paper trail (`asset-licenses/`) is exemplary.

Items are ranked highest impact → lowest, same rubric as round 1: user-visible correctness first, then crash/concurrency risk, then code health, then modernization and polish. Each item states its confidence: **confirmed** (traced or demonstrated) vs. **suspected** (mechanism solid, outcome unverified).

---

## 1. The game's jump button has no icon — `figure.jump` is not an SF Symbol · **bug (user-visible)** · confirmed

`GameView.jumpButton` renders `Image(systemName: "figure.jump")` (`Conjugar/Views/GameView.swift:983`). That symbol does not exist in the system symbol set — SwiftUI logs a **Fault** at runtime (`No symbol named 'figure.jump' found in system symbol set`, captured from the simulator console during this review) and renders nothing, so the button is an empty yellow ring on a dark circle. This is not simulator-only: the symbol set is the OS's, so devices show the same empty ring. It evaded notice because the ring still *reads* as a button, the `accessibilityLabel(L.Game.jump)` is correct (so the AX tree looks fine), and everyone testing already knew what the circle did.

A sweep of all 23 distinct `systemName:`/`systemImage:` strings in the app, widget, and Shared targets found **`figure.jump` is the only invalid one**.

**Fix (one line + a guard test):** use a real symbol: `figure.gymnastics` exists and reads as "jump". Then add a small Swift Testing case that greps the source tree for `systemName: "…"` literals (the `#filePath`-relative idiom `CorpusFormsDumpTests` already uses) and asserts `UIImage(systemName:) != nil` for each — ~25 lines, and this class of bug can never ship again.

## 2. Large widget: the pronoun column is too narrow for *nosotros*/*vosotros* · **UI bug (daily surface)** · confirmed mechanism, visual outcome needs the device eyeball

`LargeWidgetView.conjugationCell` (`ConjugarWidget/Views/LargeWidgetView.swift:133-146`) pins the pronoun to `.frame(width: 28, alignment: .trailing)` at `.caption2` with no `lineLimit`/`minimumScaleFactor`. Measured at the 11 pt system font, **"nosotros" is 46.0 pt and "vosotros" 45.3 pt wide** — neither can fit 28 pt, so rows 1p/2p of the presente grid will wrap or truncate on every systemLarge render. The blog note for this feature ends "Josh will eyeball the rendered widget on device," so this may simply not have been seen yet. The medium widget is unaffected (its `ParadigmGrid` gives pronouns natural width, `MediumWidgetView.swift:50-53`).

**Fix:** the cells already live in a `Grid` — drop the fixed frame and let the column size itself, keeping right alignment via `.gridColumnAlignment(.trailing)` on the pronoun (or, if the tight look is wanted, keep a frame but add `.lineLimit(1).minimumScaleFactor(0.5)`). Re-check with a device/gallery screenshot.

## 3. Speed/serenata power-up state survives into the boss fight — a permanent ⚡ badge over the dancer · **bug (user-visible, conditional)** · confirmed by trace

`enterBossIntro` clears the cape (`capedRemaining = 0`, `GameState+BossFight.swift:89`) but **not** `speedRemaining` / `serenataRemaining` / `serenataDanceTimer`. Those timers tick only inside the `.climb` branch of the frame tick (`GameState.swift:714-732` — the `guard phase == .climb` at :714 returns before the decrements at :723-732), so a power-up active at the triggering summit is *frozen* for the entire boss sequence. The visible consequence: `speedBadge` is in the playfield unconditionally (`GameView.swift:315`, definition :459-468) and draws whenever `speedRemaining > 0` — so a player who grabs ⚡ on stage 5 and summits within its 7 s wears a solid, never-expiring ⚡ badge through the intro, duel, victory, and end scene. (Serenata's frozen timer has no visual in the boss, and the deliberate escape-beat carry-over — decision 12 — is unaffected; during the brief escape the badge showing is consistent with "carries across.")

**Repro (device):** stage 5, collect a ⚡ pickup, reach the bull inside 7 s → the badge persists through La Llamada.
**Fix:** zero all three alongside `capedRemaining` in `enterBossIntro` (they are meaningless in the duel), and extend the `GameBossTests` boss-entry test to assert every power-up field is cleared. Alternatively gate `speedBadge` on `phase == .climb || phase == .escape` — but clearing state is the truer fix.

## 4. Music: a same-track restart during a fade-out gets hard-stopped mid-play · **bug (audio, narrow window)** · confirmed by trace

`SoundPlayerReal.stopMusic(fadeDuration:)` (`Conjugar/Utils/SoundPlayerReal.swift:123-141`) ramps the volume down, then a detached sleep task hard-stops the player, guarded by `self.musicPlayer === fadingPlayer` (:137). That guard was designed for the *track-switch* case (a different track rebuilds the player, so the identity check no-ops). But `startMusic` **reuses the same player for the same track** (:93 — the rebuild branch runs only when `currentMusic != music`), so a same-track restart inside the fade window leaves `musicPlayer === fadingPlayer` true, and the pending task kills the freshly restarted music seconds in.

Reachable today: dismiss the onboarding reshow and tap **Show Onboarding** again within ~1.5 s (`OnboardingView` fades `.onboarding` out on disappear, :188, and starts it on appear, :184) — the re-shown tour's bed dies ~1.5 s in and the rest is silent. Same shape: exiting the boss end scene (fades `.onboarding`) and reopening onboarding within ~1 s. The boss's own bossFight→onboarding handoff is safe (different tracks).

**Fix:** keep a `fadeStopTask: Task<Void, Never>?` on the player class; cancel it at the top of `startMusic` and `stopMusic()`, and check `Task.isCancelled` before the `stop()`. (A monotonically bumped generation integer captured by the task works equally well.) One more small win while in the file: the task also fires redundantly after a manual `stopMusic()` — cancellation covers that too.

## 5. Onboarding pages renumber live if tutor availability flips mid-tour · **bug (rare, first-launch adjacent)** · suspected

`OnboardingView` derives `articlesTag`/`lastPageTag` from `Current.languageModelService.isAvailable` **live in body** (`Conjugar/Views/OnboardingView.swift:43-45`) — and the view itself starts availability monitoring on appear (:183), so the flip is real: `LanguageModelServiceReal` polls every 5 s while unavailable. First launch is exactly when Apple Intelligence assets may still be downloading, and onboarding auto-presents on first launch. If availability flips from unavailable→available mid-tour: the tutor page inserts at tag 4, the articles/game pages *renumber* (5→6), the dots row grows, and a user sitting on the last page (selection 5) is silently re-pointed at the articles page while "Get Started" (keyed to `currentPage == lastPageTag`) vanishes. No crash, but a disorienting jump at the worst possible moment (a brand-new user).

**Fix:** snapshot availability once — `@State private var includeTutorPage = false`, set in `.onAppear` before anything else — and derive both tags from the snapshot. (Monitoring can then be dropped from onboarding entirely; the Info tab remains the live surface.) To see it once before fixing: onboard on a device mid-AI-download, or temporarily make the dummy service flip after 10 s.

## 6. The onboarding "Read the Articles" CTA cancels the Info tab's availability monitor · **bug (staleness, edge)** · confirmed by trace

The monitor is a single task, not refcounted: `startAvailabilityMonitoring` guards on `availabilityMonitor == nil`, `stop` cancels it unconditionally (`Conjugar/Models/LanguageModelServiceReal.swift:52-74`). Two views drive it: `InfoBrowseView` (start on appear / stop on disappear, `InfoBrowseView.swift:85-87`) and `OnboardingView` (:183/:187). The articles CTA does `router.selectedTab = .info` then `dismiss()` — the Info tab's `onAppear` fires (start) *while the cover is still up*, then the cover's `onDisappear` fires (stop). Net: the user lands on the Info tab with **no monitor running**, so the tutor row won't flip to available if they enable Apple Intelligence while sitting there — it stays stale until they leave and re-enter the tab. Only this CTA path interleaves (the other CTAs land on tabs that don't monitor), and item 5's fix removes the onboarding start… but not its stop-on-disappear.

**Fix:** make the pairing robust rather than choreographed — a refcount in the service (`monitorClients += 1` / `-= 1`, cancel at zero), or replace onAppear/onDisappear with a per-view `.task { await service.monitorWhileVisible() }` (auto-cancelling, safe under multiple concurrent owners). Either folds neatly into item 5's change.

## 7. First verb visit parses ~4 MB of content JSON on the main thread · **performance** · confirmed mechanism, magnitude suspected

`VerbView.init` synchronously loads all three content caches on first touch (`Conjugar/Views/VerbView.swift:40-45`): `Etymologies.json` (**3.0 MB**), `ExampleUses.json` (553 KB), and `MedievalExamples.json` (411 KB) — `Data(contentsOf:)` + `JSONDecoder` on the main actor, inside a NavigationStack push. This is the same shape round 1 flagged for the verb-map parse (item 14 there). The launch-time widget refresh *would* pre-warm these caches off-main (`WidgetSnapshotWriter.makeSnapshot` reads both) — but `refresh()` is **date-gated before it ever builds a snapshot** (`WidgetSnapshotWriter.swift:47-52`), so on every launch after the day's first, nothing warms and the first verb push eats the full parse. Expect a perceptible hitch on the first Browse→verb tap of a session (tens of ms decode plus dictionary bridging; unmeasured — hence "suspected" on magnitude).

**Fix (small):** warm the caches unconditionally in the existing detached launch task (`MainTabView`'s `.task` already does `Task.detached { WidgetSnapshotWriter.refresh() }` — add a `ContentCaches.warm()` beside it that touches `Etymology.text(for:)`, `ExampleData.example(for:)`, `MedievalData.examples(for:)` for any key). The caches are `nonisolated` + lock-guarded, so off-main warming is exactly what they were built for; keep the date gate for the *write* path. `VerbView.init` then costs three dictionary lookups.

## 8. iPad: rotating (or resizing) mid-game leaves the level geometry stale · **bug (iPad-only)** · suspected

`GameState.configure` builds platforms/ladders from the size at first appear and then locks (`guard !didConfigure`, `GameState.swift:488-489`; called from `GameView.onAppear`, `GameView.swift:225`). iPhone is portrait-only so this never bites there, but the app targets iPad (device family 1,2) and `Info.plist` allows **all four iPad orientations** — plus Stage Manager/Split View resizes. Rotate an iPad mid-climb and the field keeps portrait geometry inside a landscape window: platforms sized to the old width, the player clamped to old bounds, controls positioned by stale `screenSize` reads.

**Fix (decide the policy first):** cheapest is to treat a size change as a re-deal — `.onChange(of: geo.size)` → rebuild geometry and soft-respawn to safe marks (stage/score survive, like `respawn()`); fullest is proportional remapping of every entity. Worth seeing on an iPad before choosing — it may also be acceptable to defer with a known-issue note, since the game is reachable but clearly phone-shaped.

## 9. Deeplink one-shot flags go stale if they arrive while the game is open · **bug (debug surface)** · confirmed (reproduced live)

`AppRouter.handle` sets `pendingBossEntry`/`pendingEndScene` and `showGame = true` for every `conjugar://game/...` URL (`Conjugar/Views/AppRouter.swift:61-70`), but `GameView` consumes the flags only in `onAppear` (`GameView.swift:229-235`). If a `game/boss` deeplink arrives while the game is already presented, nothing consumes the flag — and the **next** plain `conjugar://game` (or Settings-independent router-path open) jumps straight to the boss. Reproduced in the simulator during this review: `openurl conjugar://game/boss` with the game up, quit, `openurl conjugar://game` → boss intro. Debug-only surface (these deeplinks are development tools), so ranked low, but it will eventually cost someone a confused ten minutes.

**Fix (one line either way):** consume-or-ignore at the router (`case "boss": if !showGame { pendingBossEntry = true }`), or clear both flags in `GameView.onDisappear`.

## 10. Dead code + stale docs around the game/content lifecycle · **code health** · confirmed

- **`GameState.reset()`'s music-restart branch is unreachable in production.** `reset()` is called only from `configure()` (fresh state, `phase == .climb`, `didConfigure` false — the condition at `GameState.swift:609-611` can't hold) and from a test. There is no boss→climb exit path anymore (leaving the boss = dismissing the game, which discards the `GameState`). Delete the branch — and correct **CLAUDE.md's** claim that `reset()` serves "configure/boss-exit paths," which will otherwise mislead the next session.
- **`Etymology.swift`'s header still says the data "is not yet displayed"** (`Conjugar/Models/Etymology.swift:18-20` — "Rendering it under the conjugations in `VerbView` … is the remaining lifecycle step"). That step shipped (VerbView's etymology card, the widget snippet). Delete the NOTE paragraph.

## 11. The "launch the game after onboarding dismisses" dance is implemented twice · **code health** · confirmed

`MainTabView` and `SettingsView` each carry an identical `pendingGameAfterOnboarding` `@State`, an identical `launchGameAfterOnboardingIfRequested()` and an identical `onDismiss` wiring (`MainTabView.swift:19, 94-95, 110-113`; `SettingsView.swift:36, 60-62, 339-343`) — plus two parallel game-presentation mechanisms (`router.showGame` vs. Settings' local `showingGame`; the parallelism itself is documented as fine, `AppRouter.swift:29-31`). The deferral plumbing is the part worth unifying: route both onboarding covers' game requests through the router (`router.requestGameAfterOnboardingDismiss()` or simply have both `onDismiss` closures flip `router.showGame`), deleting one copy of the state + helper. Small, mechanical, and the next cover-vs-cover bug only has one place to hide.

## 12. Content typography: the Spanish etymologies use French-style guillemet spacing · **polish (content)** · confirmed counts

**1,335 of 1,345** Spanish etymology entries write glosses as `« ser »` — inner spaces are the *French* convention; Spanish typography sets `«ser»` closed. (The English table has its own smaller wobble: 60 entries mix « » in among the usual “ ” — e.g. *acometer* uses both conventions in one paragraph — and one Spanish entry uses curly quotes.) Individually invisible, but this text sits directly under the conjugations on every verb screen and in the widget, and a Spanish-reading user may register it the way an English reader registers « this ». Mechanical fix in the generation pipeline + one normalization pass over the shipped JSON (`« X »` → `«X»` in `es`; unify « » → “ ” in `en`), then a ~10-line data test pinning the convention so future batches can't drift back.

## 13. Micro cleanups · **polish (grab-bag)** · confirmed unless noted

- **Unseeded RNG in the boss's judged path:** the dancer's spoken jaleo picks with plain `.randomElement()` (`GameState+BossFight.swift:259`) while every other boss/climb draw routes through `bossRNG` (`:193, :469, :479`). Cosmetic-only today, but it's the one hole in the otherwise-complete "tests can script the fight" story — pass `using: &bossRNG` for consistency.
- **Scene beds start at a random playhead:** `startMusic` seeks a fresh track to a random position (`SoundPlayerReal.swift:109`). Great for the gameplay loop's variety; for the *composed* beds (`.onboarding`, `.bossFight`) it means entering mid-phrase. If that's deliberate, a comment saying so would stop it reading as an oversight; if not, start those two at 0 (a `Music` property, e.g. `startsAtRandomPosition`).
- **`resolveCollisions` ends with `if resolveChargerCollisions() { return }`** (`GameState+Obstacles.swift:183`) — the `return` is the last statement, so the conditional is a no-op costume; call it plainly and keep the explanatory comment (or move the call earlier if the early-out is meant to guard future code).
- **Simulator aside (not an app bug):** stage 1's flag obstacles render as "?" tofu boxes in the iOS Simulator — the sim's emoji font ships without regional-indicator flags; devices render them fine. Worth remembering when generating App Store screenshots (stage 1 will look broken from the sim; capture device screenshots or use `CONJUGAR_GAME_STAGE=2+`).

## 14. Create (or, less likely, source) a real olé glyph for the boss dance pad · **polish (game UI)** · follow-up from review discussion

The olé button and its matching cue chip currently use `figure.mind.and.body` (`Conjugar/Views/GameView.swift:810-813`; `moveChip`, `:596-600`). It renders fine and passed the item-1 symbol sweep, but it reads as *seated meditation*, not a desplante — it prompted a "what does this button mean?" during this review. No SF Symbol captures the pose (the nearest stock options, `figure.dance` and `figure.mixed.cardio`, are both compromises), so the plan is a **custom symbol**: the arms-up-V + back-arch silhouette, ideally derived from the game's own `dancer_ole_3` peak frame, or built through the proven tab-bar pipeline (the custom `dancer`/`bull` symbols were spliced from line art into an SF template and weight-matched to the stock glyphs). Sourcing a suitable line icon (Noun Project, as with the tab icons) is the fallback.

**Pose references** (for creating/judging the glyph — not shipped in the game):

- *Flamenco en México, bailaora 3* — the arms-up-V desplante, closest to the sprite. Page: <https://commons.wikimedia.org/wiki/File:Flamenco_en_M%C3%A9xico,_bailaora_3.jpg> · file: <https://upload.wikimedia.org/wikipedia/commons/7/7d/Flamenco_en_M%C3%A9xico%2C_bailaora_3.jpg> (Armartinell, CC BY 4.0)
- *Woman Flamenco dancer* — the floreo wrist-curl detail for the hands. Page: <https://commons.wikimedia.org/wiki/File:Woman_Flamenco_dancer.jpg> (Raymonst3, CC BY-SA 4.0)
- John Singer Sargent, *Spanish Dancer* — the arched-back drama (arms are one-out/one-down, so pose-mood reference only). Page: <https://commons.wikimedia.org/wiki/File:Sargent_John_Singer_Spanish_Dancer.jpg> (public domain)

Licensing note: using these as *pose reference* for freshly drawn art carries no obligation; a direct **trace** of either CC photo is a derivative and would need an `asset-licenses/` credit like the Noun Project tab icons (prefer the CC **BY** photo over the BY-**SA** one in that case — share-alike is awkward in the asset set). Mechanics of the swap: change the pad button and cue chip **in lockstep** (both already accept non-system images — the cape button/chip use `Image("cape_pickup")`), and consider hoisting the per-move glyph into a `DanceMove` property beside `cueSound` so the two sites can't diverge. Keeps the item-1 constraint intact: jump (`figure.gymnastics`) and olé stay visually distinct. Accessibility labels (`L.Game.oleMove`) are unaffected.

---

## What was checked and came back clean

Deep-read with no findings: the climb physics/collision pipeline (`GameState+Physics/+Obstacles`), the stage system's invariants (`stage == summitCount + 1`, bag draws, escape/respawn state hygiene — the deliberate escape-beat power-up carry is correctly asymmetric with death), the three challenge mechanics (the apagón envelope being a pure function of `mechanicRemaining` is particularly nice), the boss-fight state machine (freeze slot never first, echo/freeze timing races handled, the bow-hold cap, idempotent llamada), `SoundPlayerReal`'s off-main warm-up/playback architecture (item 4 aside), `HapticPlayerReal`'s held-generator pattern, the `World` wiring of the two new services with proper Dummy conformers, `Settings`' appIcon persistence + live-swap guard, `AppIcon`, the `xcstrings` additions (every `L` accessor's runtime key resolves in the catalog, both languages, verified mechanically), the content caches' `@unchecked Sendable` lock discipline, the widget snapshot's backward-compatible optional fields and the tilde-rebalancing truncation, and the corpus pipeline's test-target tooling. The game's ~100 new tests are genuinely good — seeded-RNG scripting of exact boss phrases is the standout.

## Proposed implementation sequence

Every step ships green (build + 508 tests + lint); bugs land before refactors; one behavior-decision item is isolated at the end.

1. **One-line fixes + guard tests** *(items 1, 3, 9, 13's RNG + return-idiom nits)* — swap the jump symbol and add the symbol-validity sweep test; clear speed/serenata state in `enterBossIntro` + extend the boss-entry test; consume-or-ignore the deeplink flags; seed the jaleo pick. All mechanical, each independently verifiable in the simulator (the game bits by deeplink, no play-through needed).
2. **Docs + dead code** *(item 10)* — delete `reset()`'s unreachable music branch, fix the CLAUDE.md sentence, drop `Etymology.swift`'s stale NOTE. Zero behavior change.
3. **Audio-stack pass** *(item 4 + 13's playhead decision)* — the cancellable fade-stop task, plus decide/document the random-start policy for scene beds. One file; verify by ear with the onboarding reshow double-open.
4. **Onboarding/tutor pass** *(items 5, 6, then 11)* — snapshot availability at onboarding appear, make monitor ownership refcounted/task-scoped, then collapse the duplicated game-after-onboarding plumbing into the router. These three touch the same seams; doing them together avoids re-testing the flow twice.
5. **Perf + widget** *(items 7, 2)* — warm the content caches in the launch task (verify: first verb push after a warm launch has no hitch); fix the pronoun column and re-eyeball the large widget on device. Independent of everything else.
6. **iPad geometry policy** *(item 8)* — the one item with a real design decision (re-deal vs. remap vs. documented limitation). Needs an iPad look first; keep it last so it can't block the mechanical wins.
7. **Content normalization** *(item 12)* — the guillemet pass over `Etymologies.json` + the convention-pinning data test. Mechanical but a large diff; isolate it in its own commit so `git blame` on the content stays readable.
8. **Olé glyph** *(item 14)* — independent of everything above; slot it whenever the symbol/sprite pipeline is warm (it pairs naturally with item 1's jump swap if done early, but nothing blocks on it). Asset work first, then the two-site (button + chip) swap and the optional `DanceMove` glyph-property hoist.

Steps 1–2 are an easy morning; steps 3–5 are each an hour or two with verification; step 6 is bounded by the decision, not the code; step 8 is mostly art time. Independently of this list, round 1's one still-open external item — the Game Center physical-device pass — remains outstanding.

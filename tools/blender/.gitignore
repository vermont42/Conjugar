# Generated render output — the shipped frames live in Assets.xcassets, not here.
renders/
dist/

# Raw source meshes/animations (e.g. Mixamo FBX). Not committed: Mixamo's license
# forbids redistributing raw assets standalone, and this repo is public (AGPL).
# Keep these locally in source/; log provenance in asset-licenses/.
source/
*.fbx
*.blend
*.blend1

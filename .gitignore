# TelemetryDeck app ID — copy Conjugar/Secrets.example.xcconfig to
# Conjugar/Secrets.xcconfig and fill in your app ID. Never commit Secrets.xcconfig.
Conjugar/Secrets.xcconfig

# Other
.DS_Store
docs/screenshots/
Conjugar.xcodeproj/project.xcworkspace/xcuserdata/
Conjugar.xcodeproj/xcuserdata/

build/
dist/
node_modules/

# Python
__pycache__/
*.pyc

# Commercial reference PDFs (do not redistribute)
*.pdf

# Gloss-pipeline scratch artifacts (preserved in ~/Desktop/workspace/Migration)
docs/glosses/.bak_phase3/
docs/glosses/phase1/phase2/chunks/
docs/glosses/phase1/phase2/checks/
docs/glosses/phase1/phase2/consensus/
# ios-build-verify
# .claude/ios-build-verify.config.sh is deliberately NOT ignored: it holds only project
# facts (scheme, bundle ID, launch anchor, the hand-measured 5-tab coordinates), and a
# clone needs it for the documented build path to work.
build.log

# Claude Code per-machine state (tool allowlist, scheduler lock) — not shared
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Original purchased audio (Pond5 WAV masters — converted MP3s are committed instead)
audio-sources/
